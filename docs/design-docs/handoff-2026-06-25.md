# Session handoff — 2026-06-25

Continuity doc across several workstreams. Read this first after a context compaction.

> **Canonical docs location: the `integration` branch.** All internal docs (design-docs, `superpowers/plans`, this handoff, the roadmap) live here — `integration` is the superset of every branch's docs. Update docs *here* going forward. Feature branches (`feat/*`) carry frozen snapshots; upstream PR branches (`pr/*`) carry only user-facing `.mdx` setup docs (code-only otherwise). See the Branch map below.

## TL;DR — current status (updated 2026-06-29)

**Microsoft Teams channel adapter** on branch `feat/teams-channel` — **v1, v1.2, v2a, v2b AND the DM `"*"` wildcard ALL SHIPPED + reviewed + pushed** to `origin/feat/teams-channel` (HEAD `81acc668`). `cargo test --lib` 970/0. **VALIDATED end-to-end against real Microsoft Teams on 2026-06-29.** Do NOT re-execute the v1/v1.2/v2a/v2b plans — they're done.

**Now being upstreamed to `spacedriveapp/spacebot`** (sliced backend + UI):
- **PR #607** — Teams adapter (backend + setup docs). **OPEN.** CodeRabbit review addressed (`9a94d957`/`104df402`): single-instance routing fix, hot-reload warning, doc comment; the "fail-open on disable" finding declined as cross-adapter parity. Branch `pr/teams-adapter` off `main`, `just gate-pr` green.
- **PR #608** — Teams in the Channels UI. **DRAFT**, stacked on #607 (rebase onto `main` + mark ready once #607 merges). Branch `pr/teams-channels-ui`.
- **PR #605** — `"*"` wildcard alignment for Slack/Mattermost/Twitch. **OPEN.** Branch `pr/dm-allow-all-wildcard`.

**Everything not built yet is consolidated in `teams-roadmap.md` → "Deferred / out-of-scope — tracked backlog"** (outbound/inbound reactions, select menus, streaming, multi-bot, v3 approval cards, cross-adapter hot-reload teardown, richer identity, facilitation).
- **v1 (done + REAL-WORLD VALIDATED 2026-06-26):** single Teams bot, text + @mention. Inbound axum `/api/messages` + JWT validation, outbound respond/broadcast (SSRF-guarded), config like Slack, target.rs, mention detection. Plan: `docs/superpowers/plans/2026-06-25-teams-channel-adapter.md`. **Validated end-to-end against a real Azure Bot via a cloudflared tunnel** — real JWT accepted, agent replied in Teams, outbound Bot Connector POST OK; no code bugs. **⚠️ Before deploying on the live instance, rebase `feat/teams-channel` onto current `main`** — the branch lacks migration `20260623000001` that the live DB has (sqlx refuses the older binary; the test used an isolated instance dir to dodge it).
- **v1.2 (done):** connect/delete/status a Teams bot from the **Settings → Channels UI** (single-instance) + watcher hot-start parity + `build_teams_adapter` helper. Plan: `docs/superpowers/plans/2026-06-26-teams-channels-ui.md`. Follow-ups I-1/M-1 resolved.
- **v2a (done):** outbound Adaptive Cards (`RichMessage.cards`), typing (`send_status` refresh loop), inbound file/image attachments (`MessageContent::Media`, bearer host-gated). Unified SSRF-gated send seam underneath. Plan: `…teams-v2a.md`. 5 commits `004d79e8..f2c0ead9`.
- **v2b (done):** card-button interactions — outbound `interactive_elements` → Adaptive Card actions; inbound `Action.Submit` `value` → `MessageContent::Interaction`. Deferred: thread replies (already work), outbound reactions (need Graph), select menus. Plan: `…teams-v2b.md`. Commits `0b9430df..d2a6d3ff`.
- **DM `"*"` wildcard (done):** `dm_allowed_users = ["*"]` allows all DMs on Teams (commit `c0284293`); empty still blocks. Upstream cross-adapter alignment (Slack/Mattermost/Twitch) = **PR #605** (`spacedriveapp/spacebot`, OPEN, awaiting maintainers).
- **✅ VALIDATED live 2026-06-29** (isolated `SPACEBOT_DIR` + cloudflared + real Azure Bot F0): real JWT (JWKS 312 keys); wildcard let the sender through with no MRI allowlisting; inbound image → `Media` downloaded with bearer to allowlisted `*.trafficmanager.net` (**v2a SSRF bearer-gate confirmed live**, 349845 B saved); Adaptive Cards rendered; **button click → `Interaction` round-trip** (agent reacted); token mint OK (secret valid). Zero code bugs; only friction was Azure endpoint propagation lag. Torn down; real config/DB untouched.
- **v3 approval cards — channel-agnostic design done** (`docs/design-docs/approval-cards-design.md`, Opus-reviewed). NOT built. Open decisions O1 (approver-allowlist source) / O2 (`sender_id` vs `aadObjectId`) / O4 (Backlog vs terminal Rejected) to settle before planning.
- **NOT built (designed):** v1.1 multi-bot (shared listener demux by JWT aud), v2c (streaming), v3 approval cards. Each gets its own plan.
- **Disk guard added** (`.claude/hooks/disk-guard.sh` + `settings.local.json`) after 2 disk-full incidents — blocks heavy cargo builds under 25 GiB free; activates on session restart.
- **`teams-setup.md` is now battle-tested** (real gotchas: subscription/F0, manifest `packageName`/`id`/v1.17, admin-approval+restrict install path, DM-MRI finding) + an embedded copy-paste manifest generator.
- **Real-world tests (2026-06-26 v1, 2026-06-29 v2a/v2b/wildcard) torn down**: isolated instances removed, spacebot+cloudflared stopped, real config/DB never touched. User KEEPS the Azure artifacts (Entra app `872cdb70…` + Azure Bot F0 `Spacebot-Teams` + Teams app **restricted to one account**) for re-testing — the Azure messaging endpoint points at a now-dead tunnel, reconfigure at the next test. **Rotate the client secret before prod** (it transited chat; expires 2026-07-04 — still valid, live token mint confirmed on 2026-06-29).

## NEXT — resume here (Teams backlog; full detail in `teams-roadmap.md`)

1. **Shepherd the open upstream PRs** (`spacedriveapp/spacebot`): **#607** (Teams adapter — review addressed, awaiting maintainers), **#608** (Channels UI — DRAFT; on #607 merge → rebase onto `main` + mark ready), **#605** (wildcard alignment). Watch for new CodeRabbit/Tembo findings; if Tembo re-mangles a PR description, re-restore it. Verify each finding against code before fixing (~35% false). Propagate any fix to all three branches + `feat/teams-channel`.
2. **Before any real Teams deploy:** (a) **rotate the client secret**; (b) **rebase `feat/teams-channel` onto current `main`** (migration `20260623000001` line — the isolated-instance tests dodged it).
3. **The full deferred/out-of-scope backlog is in `teams-roadmap.md` → "Deferred / out-of-scope — tracked backlog"** — reactions (in/out), select menus, streaming (v2c), multi-bot (v1.1), v3 approval cards, cross-adapter hot-reload teardown, richer identity, facilitation. Pick from there.
4. **v3 approval cards** is the biggest next build: settle O1/O2/O4 in `approval-cards-design.md`, then `writing-plans` → Opus review → execute.

Method for each: `writing-plans` → Opus review → subagent-driven execution → real-world re-test via cloudflared (artifacts kept).

## Branch map (authoritative)

| Branch | What | Pushed? | Notes |
|---|---|---|---|
| `main` | = `origin/main` = `upstream/main` (spacedriveapp/spacebot). Pristine. | yes | **DECIDED (2026-06-29): keep `main` pristine** — never merge feature branches into it (preserves clean upstream sync + clean PR diffs). The deploy/aggregate line is `integration`, not `main`. |
| **`integration`** | **DEPLOY / aggregate line** = `main` + `feat/surrealdb-memory` (memory + SurrealDB backend) + `feat/teams-channel` (Teams adapter v1/v1.2/v2a/v2b + DM wildcard) + `pr/dm-allow-all-wildcard` (Slack/MM/Twitch wildcard, #605). This is what you run/deploy. | yes (`origin`) | `cargo test --lib` 995/0, fmt + clippy clean. Re-merge `main` (upstream) periodically; merge new feature branches here. |
| `feat/surrealdb-memory` | **Feature source** — SurrealDB memory work + `MemoryBackend` abstraction + bug fixes + design docs. (Was the old deploy line; superseded by `integration`.) | yes (`origin`) | Has the review-fix back-port (commit `91108117`). |
| `pr/ingestion-fixes` | PR A — ingestion reliability + merge-bloat fixes (cross-backend). | yes | → **spacedriveapp/spacebot#604** (upstream, OPEN). Bot reviews addressed (delete-order, merge cap, path-guard, rustfmt). Awaiting human review. |
| `pr/memory-abstraction` | PR B — `MemoryBackend` trait (pluggable/storage-agnostic). | yes | → **slvnlrt/spacebot#1** (DRAFT in fork, base = `pr/ingestion-fixes`). Reviews addressed (agent_id, deterministic ordering, IN-chunking, FTS); 1 "Major" (cross-store save atomicity) declined w/ rationale. |
| `feat/teams-channel` | **Feature source.** Full Teams adapter (v1 + v1.2 + v2a + v2b + DM wildcard) + all Teams docs/plans. Off `main`. Real-world validated 2026-06-29; merged into `integration`. | yes (`origin`, HEAD `81acc668`) | Trailer-free, `cargo test --lib` 970/0. |
| `pr/teams-adapter` | **PR #607** (upstream, OPEN) — Teams adapter backend + setup docs, code-only off `main`. | yes (`origin`, HEAD `104df402`) | Trailer-free, `just gate-pr` green. CodeRabbit review addressed. |
| `pr/teams-channels-ui` | **PR #608** (upstream, DRAFT) — Teams Channels UI, stacked on #607. | yes (`origin`, HEAD `3ed93347`) | Rebase onto `main` + mark ready when #607 merges. |
| `pr/dm-allow-all-wildcard` | **PR #605** (upstream, OPEN) — `"*"` wildcard alignment (Slack/Mattermost/Twitch), code-only off `main`. | yes (`origin`) | Trailer-free, gate green. |

## Workstream 1 — Upstreaming the memory work (in flight)

Strategy: stack `main → A (fixes) → B (abstraction) → C (SurrealDB backend)`, smallest/safest first. Tracker: `docs/design-docs/upstreaming-tracker.md` (on `feat/surrealdb-memory`).
- **PR A #604**: open upstream, awaiting review.
- **PR B #1**: draft in fork, stacked on A. **When #604 merges:** rebase B onto `main`, retarget to upstream, mark ready (diff becomes the single abstraction commit).
- **PR C** (SurrealDB backend): NOT started — deferred until B lands / upstream interest. Extract off `pr/memory-abstraction`.
- Review fixes are **back-ported to `feat/surrealdb-memory`** so the deployed branch isn't running the caught bugs.

## Workstream 2 — Cortex memory consolidation (Phase 4) — future dedicated session

The `memory_consolidate` cortex routine is **designed but unbuilt** (`prompts/en/cortex.md.j2` + `docs/design-docs/cortex-implementation.md` Phase 4 reference a tool that doesn't exist). 
- Cartography: `docs/design-docs/memory-consolidation-cartography.md`. Kickoff brief: `docs/superpowers/plans/2026-06-23-cortex-consolidation-kickoff.md` (both on `feat/surrealdb-memory`).
- Build with the **3 frontier mechanics inscribed as constraints**: bi-temporal validity, contradiction *resolution* (not just flagging), anti-drift/idempotency anchoring against the immutable episode ledger. Plus cheap-then-escalate + graph-aware clustering.
- This is the better home for "dedup/consolidation" than write-time (the dropped B4).

## Workstream 3 — Teams adapter (v1 + v1.2 SHIPPED — see TL;DR)

> **Status:** v1 (text + @mention) and v1.2 (Channels UI + watcher parity) are DONE, reviewed, and pushed (`3dca3886`). The notes below are the original v1 design context, kept for reference. Future work = the v1.1/v2/v3 roadmap (`docs/design-docs/teams-roadmap.md`). Do not re-execute v1.

- **Approach:** Bot Framework (confirmed by research + the Hermes/NousResearch impl). Hand-rolled Rust: `axum` inbound `POST /api/messages` (own server, `webhook.rs` convention) + JWT validation (`jsonwebtoken` vs Bot Framework JWKS) + `azure_identity`/`reqwest` outbound to the Bot Connector + a **`serviceUrl` sidecar** (Hermes pattern) for proactive sends.
- **v1 scope:** config (Slack-shaped, named instances) + `TeamsPermissions` (bindings integration) + inbound (JWT + Activity→`InboundMessage`) + `respond` **Text** + `broadcast` + `target.rs` (3 touchpoints) + manager registration + the `teams` mention-detection arm in `channel.rs` + setup doc. **v2** = other capabilities (attachments/interactions/threads/cards/streaming) — each maps onto the EXISTING `MessageContent`/`OutboundResponse` vocabulary (no new variants). **v3** = human-approval Adaptive Cards (builds on the existing `TaskStatus::PendingApproval` + `task_update(approved_by)` gate).
- **Capability vocabulary already exists end-to-end** (`MessageContent::{Text,Media,Interaction}`, produced by Slack/Discord, consumed by the agent) — Teams normalizes onto it; minimal core touch = adding `teams` to the mention source list.
- **⚠ Prerequisite for end-to-end test:** Teams requires a **public HTTPS endpoint** reachable by Microsoft. Current deploy (SSH tunnel, bind 127.0.0.1) is insufficient → need a public reverse-proxy/tunnel (Caddy/cloudflared). Build + unit tests work without it.
- Research artifacts (in conversation): Hermes Agent (NousResearch) uses Bot Framework + the patterns we borrow; the general feasibility survey confirmed Bot Framework is the only bidirectional path (Graph subscriptions = passive; incoming-webhooks = outbound-only + O365 connectors retired May 2026; Teams SDK = no Rust).

## Standing constraints / gotchas (CRITICAL — carry forward)

- **Commit/push ONLY when the user asks.** Add files explicitly (no `git add -A` — repo has a reserved `nul` file).
- **Upstream PR branches:** code-only (no `docs/`/`.superpowers/`/`spikes/`), off `main`, no internal jargon (bug-number tags, "Lot", gap-analysis), **no surreal mentions in code**, no commit trailers (no Co-Authored-By/session link). Must pass `cargo fmt --all -- --check` + `cargo test --lib` + the feature-on `clippy` job (CI enforces fmt — always fmt-check before opening/pushing).
- **Heavy cargo wrapped:** `systemd-run --scope -p MemoryMax=40G -p MemorySwapMax=0 …`.
- **`sda` 1.7T = the Proxmox HOST disk — DO NOT TOUCH.** We run inside a Docker container (overlay `/dev/loop0`, was ~41G free after purging the 6.6G `spikes/surreal-memory/target`).
- **Verify agent/bot findings against the code before acting (~35% false/exaggerated).** Both Opus plan reviews caught real defects; the Explore agent made one false claim (corrected).
- **Research subagents: WebSearch/WebFetch only, NEVER Context7** (MCP schemas overflow context → crash).
- **spacebot daemon:** `./target/release/spacebot start` (daemonizes; bind 127.0.0.1:19898). Verify a build is feature-on with `./target/release/spacebot migrate-memory --help` (present = feature-on; a `--features surreal-memory` build can silently land feature-OFF → falls back to SQLite). Build the frontend (`cd interface && bun run build`) BEFORE the binary or rust-embed ships an empty UI. **Currently STOPPED.**
- OpenRouter key is plaintext in `/root/.spacebot/config.toml` — flagged to move to the secrets store; don't echo it.

## Open decisions / next actions

1. **Teams (current):** execute the v1 plan (Tasks 1→11) subagent-driven. Arrange a public HTTPS endpoint before the end-to-end test.
2. **#604** awaits upstream review; when it merges → rebase + retarget PR B #1.
3. **Merge-to-main** — **DECIDED (2026-06-29):** keep `main` pristine (= upstream mirror); the `integration` branch is the deploy/aggregate line (`main` + memory/surreal + teams). Feature branches merge into `integration`, never into `main`; upstream PRs stay branched off `main`.
4. **Cortex Phase-4 consolidation:** separate dedicated session when ready.
5. Several docs uncommitted only on `feat/surrealdb-memory`? No — they were committed + pushed there. The Teams plan + this handoff are on `feat/teams-channel`.
